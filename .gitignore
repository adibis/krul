# Build output
zig-out/
.zig-cache/

# Models — large binary files, track separately or via Git LFS
models/*.onnx
models/*.onnx.data
models/vocab.bin
models/merges.bin

# Local config (may contain secrets)
krul.toml

# PostgreSQL temp files
*.pid

# Planning docs — local only, not for version control
ARCHITECTURE.md
CRITIQUE.md
HERMES_COMPARISON.md
GEAR_01_MODEL.md
PLAN.md
PROMPT.md
TUI_PLAN.md

# External / sibling projects — developed separately
ext/

# Research paper (separate from daemon source)
paper/
Archive.zip

# Python
__pycache__/
*.pyc

# OS
.DS_Store
